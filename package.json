{
  "name": "starboard-monorepo",
  "version": "0.0.1",
  "private": true,
  "files": [],
  "license": "MPL-2.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --sort",
    "lerna": "lerna",
    "server:start": "flask run --host=0.0.0.0"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "pandas": "^0.0.3"
  }
}
